# 📁 项目结构说明

## 🌳 完整目录树

```
F:\SearchTool\
├── 📄 Cargo.toml                    # Rust项目配置文件
├── 📄 tauri.conf.json               # Tauri应用配置
├── 📄 build.rs                      # 构建脚本
├── 📄 .gitignore                    # Git忽略文件
│
├── 📚 文档/
│   ├── 📄 README.md                 # 项目说明
│   ├── 📄 快速开始指南.md           # 使用教程
│   ├── 📄 项目改写完成总结.md       # 技术总结
│   ├── 📄 项目概览.md               # 项目概览
│   └── 📄 项目结构说明.md           # 本文档
│
├── 🦀 src/                          # Rust源代码
│   ├── 📄 main.rs                   # 程序入口
│   ├── 📄 lib.rs                    # Tauri应用主逻辑
│   ├── 📄 config.rs                 # 配置管理模块
│   ├── 📄 database.rs               # 数据库层模块
│   ├── 📄 scanner.rs                # 文件扫描模块
│   ├── 📄 watcher.rs                # 文件监控模块
│   ├── 📄 commands.rs               # Tauri命令模块
│   ├── 📄 hotkey.rs                 # 全局热键模块
│   └── 📄 utils.rs                  # 工具函数模块
│
├── 🌐 ui/                           # 前端界面
│   ├── 📄 index.html                # 主界面HTML
│   ├── 📄 styles.css                # 样式表
│   └── 📄 app.js                    # 应用逻辑JS
│
├── 🖼️ icons/                        # 应用图标
│   ├── 📄 32x32.png
│   ├── 📄 128x128.png
│   ├── 📄 128x128@2x.png
│   ├── 📄 icon.icns
│   ├── 📄 icon.ico
│   └── 📄 README.md
│
├── 🔧 rust_project/                 # 原USN引擎（保留）
│   ├── 📄 Cargo.toml
│   └── src/
│       └── 📄 lib.rs                # USN扫描引擎
│
├── 🗃️ target/                       # 编译输出（.gitignore）
│   ├── debug/                       # 调试版本
│   └── release/                     # 发布版本
│       ├── 📦 filesearch.exe        # 可执行文件
│       └── bundle/                  # 安装包
│
├── 📊 2250_高性能版.py              # 原Python版本（参考）
└── 📦 file_scanner_engine.dll       # 原DLL（参考）
```

## 📂 核心目录详解

### 1. src/ - Rust源代码目录

#### main.rs
```rust
// 程序入口点
// 职责：启动Tauri应用
// 代码量：~5行
```

#### lib.rs
```rust
// Tauri应用主逻辑
// 职责：
// - 应用状态管理
// - 系统托盘设置
// - 全局热键注册
// - 窗口事件处理
// - 命令处理器注册
// 代码量：~150行
```

#### config.rs
```rust
// 配置管理模块
// 职责：
// - 配置文件读写
// - 默认配置生成
// - 搜索历史管理
// - 收藏夹管理
// - C盘路径配置
// 代码量：~200行
```

#### database.rs
```rust
// 数据库层模块
// 职责：
// - SQLite连接管理
// - 表结构初始化
// - FTS5全文搜索
// - 批量插入优化
// - 索引管理
// - 统计信息
// 代码量：~500行
```

#### scanner.rs
```rust
// 文件扫描模块
// 职责：
// - USN Journal扫描
// - MFT记录解析
// - 路径映射构建
// - 目录递归扫描
// - 文件过滤
// 代码量：~800行
```

#### watcher.rs
```rust
// 文件监控模块
// 职责：
// - USN变更监控
// - 文件变更检测
// - 增量索引更新
// - 事件通知
// 代码量：~300行
```

#### commands.rs
```rust
// Tauri命令模块
// 职责：
// - 30+ Tauri命令
// - 前后端通信接口
// - 参数验证
// - 错误处理
// 代码量：~1000行
```

#### hotkey.rs
```rust
// 全局热键模块
// 职责：
// - Windows热键注册
// - 热键事件处理
// - 跨平台兼容
// 代码量：~150行
```

#### utils.rs
```rust
// 工具函数模块
// 职责：
// - 文件大小格式化
// - 时间格式化
// - 路径处理
// - 扩展名提取
// - 模糊匹配
// - 驱动器枚举
// 代码量：~200行
```

### 2. ui/ - 前端界面目录

#### index.html
```html
<!-- 主界面HTML -->
<!-- 结构：
  - Header: 标题栏、搜索栏、筛选栏
  - Main: 结果表格、分页栏
  - Footer: 状态栏
  - Context Menu: 右键菜单
-->
<!-- 代码量：~200行 -->
```

#### styles.css
```css
/* 样式表 */
/* 包含：
  - 全局样式
  - 组件样式
  - 主题变量
  - 响应式设计
  - 动画效果
*/
/* 代码量：~500行 */
```

#### app.js
```javascript
// 应用逻辑JS
// 功能：
// - 初始化
// - 搜索功能
// - 结果渲染
// - 筛选排序
// - 分页控制
// - 事件处理
// - Tauri通信
// 代码量：~800行
```

### 3. icons/ - 图标目录

```
icons/
├── 32x32.png           # 小图标
├── 128x128.png         # 中图标
├── 128x128@2x.png      # 高DPI图标
├── icon.icns           # macOS图标
├── icon.ico            # Windows图标
└── README.md           # 图标说明
```

### 4. rust_project/ - 原引擎目录

```
rust_project/
├── Cargo.toml          # 原项目配置
├── src/
│   └── lib.rs          # USN扫描引擎（1528行）
└── target/             # 原编译输出
```

## 🔗 模块依赖关系

```
main.rs
  └─→ lib.rs
       ├─→ config.rs
       ├─→ database.rs
       ├─→ scanner.rs
       │    └─→ usn_engine (内嵌)
       ├─→ watcher.rs
       │    ├─→ database.rs
       │    └─→ scanner.rs
       ├─→ commands.rs
       │    ├─→ config.rs
       │    ├─→ database.rs
       │    ├─→ scanner.rs
       │    ├─→ watcher.rs
       │    └─→ utils.rs
       ├─→ hotkey.rs
       └─→ utils.rs
```

## 📊 代码统计

### 按语言
| 语言 | 文件数 | 代码行数 | 百分比 |
|------|--------|----------|--------|
| Rust | 9 | ~4000 | 75% |
| JavaScript | 1 | ~800 | 15% |
| HTML | 1 | ~200 | 4% |
| CSS | 1 | ~500 | 9% |
| **总计** | **12** | **~5300** | **100%** |

### 按模块
| 模块 | 代码行数 | 占比 |
|------|----------|------|
| commands.rs | ~1000 | 25% |
| scanner.rs | ~800 | 20% |
| database.rs | ~500 | 12.5% |
| watcher.rs | ~300 | 7.5% |
| config.rs | ~200 | 5% |
| utils.rs | ~200 | 5% |
| lib.rs | ~150 | 3.75% |
| hotkey.rs | ~150 | 3.75% |
| app.js | ~800 | 20% |
| styles.css | ~500 | 12.5% |
| index.html | ~200 | 5% |

## 🗂️ 运行时文件

### 用户目录 (~/.filesearch/)
```
~/.filesearch/
├── config.json         # 配置文件
├── index.db            # 索引数据库
├── index.db-shm        # SQLite共享内存
├── index.db-wal        # SQLite预写日志
└── app.log             # 应用日志
```

### 配置文件示例
```json
{
  "search_history": ["关键词1", "关键词2"],
  "favorites": [
    {"name": "桌面", "path": "C:\\Users\\...\\Desktop"}
  ],
  "theme": "light",
  "c_scan_paths": {
    "paths": [
      {"path": "C:\\Users\\...\\Desktop", "enabled": true}
    ],
    "initialized": true
  },
  "enable_global_hotkey": true,
  "minimize_to_tray": true,
  "auto_start_watcher": true
}
```

### 数据库结构
```sql
-- 主表
CREATE TABLE files (
    id INTEGER PRIMARY KEY,
    filename TEXT NOT NULL,
    filename_lower TEXT NOT NULL,
    full_path TEXT UNIQUE NOT NULL,
    parent_dir TEXT NOT NULL,
    extension TEXT,
    size INTEGER DEFAULT 0,
    mtime REAL DEFAULT 0,
    is_dir INTEGER DEFAULT 0
);

-- FTS5表
CREATE VIRTUAL TABLE files_fts 
USING fts5(filename, content=files, content_rowid=id);

-- 元数据表
CREATE TABLE meta (
    key TEXT PRIMARY KEY,
    value TEXT
);
```

## 🔨 构建产物

### Debug构建
```
target/debug/
├── filesearch.exe      # 可执行文件（未优化）
├── filesearch.pdb      # 调试符号
└── deps/               # 依赖库
```

### Release构建
```
target/release/
├── filesearch.exe      # 可执行文件（优化）
├── filesearch.pdb      # 调试符号（可选）
└── bundle/             # 安装包
    ├── msi/
    │   └── filesearch.msi
    └── nsis/
        └── filesearch-setup.exe
```

## 📦 依赖关系

### Cargo.toml依赖
```toml
[dependencies]
tauri = "1.5"              # 应用框架
rusqlite = "0.31"          # 数据库
rayon = "1.8"              # 并行处理
parking_lot = "0.12"       # 高性能锁
rustc-hash = "2.0"         # 快速哈希
serde = "1.0"              # 序列化
serde_json = "1.0"         # JSON
tokio = "1"                # 异步运行时
chrono = "0.4"             # 时间处理
anyhow = "1.0"             # 错误处理
log = "0.4"                # 日志
env_logger = "0.11"        # 日志实现
dirs = "5.0"               # 目录
bincode = "1.3"            # 二进制序列化
crossbeam-channel = "0.5"  # 通道
```

## 🎯 关键文件说明

### Cargo.toml
- **作用**：Rust项目配置
- **内容**：依赖、构建配置、元数据
- **重要性**：⭐⭐⭐⭐⭐

### tauri.conf.json
- **作用**：Tauri应用配置
- **内容**：窗口、托盘、权限、打包
- **重要性**：⭐⭐⭐⭐⭐

### build.rs
- **作用**：构建脚本
- **内容**：Tauri构建逻辑
- **重要性**：⭐⭐⭐

### .gitignore
- **作用**：Git忽略规则
- **内容**：target/、*.db等
- **重要性**：⭐⭐⭐⭐

## 📝 文件命名规范

### Rust文件
- 小写+下划线：`file_name.rs`
- 模块名与文件名一致
- 测试文件：`tests/`目录

### 前端文件
- 小写+连字符：`file-name.html`
- 样式文件：`.css`
- 脚本文件：`.js`

### 文档文件
- 中文+`.md`：`项目说明.md`
- 英文大写：`README.md`

## 🔍 快速定位

### 需要修改配置？
→ `src/config.rs` + `~/.filesearch/config.json`

### 需要修改UI？
→ `ui/index.html` + `ui/styles.css` + `ui/app.js`

### 需要修改搜索逻辑？
→ `src/database.rs` + `src/scanner.rs`

### 需要修改文件操作？
→ `src/commands.rs`

### 需要修改热键？
→ `src/hotkey.rs` + `src/lib.rs`

### 需要修改托盘？
→ `src/lib.rs` (create_system_tray)

## 🎓 学习路径

### 新手入门
1. 阅读 `README.md`
2. 阅读 `快速开始指南.md`
3. 运行 `cargo tauri dev`
4. 修改 `ui/` 文件体验

### 进阶开发
1. 阅读 `src/lib.rs`
2. 理解模块依赖
3. 阅读各模块代码
4. 修改 `src/` 文件

### 高级定制
1. 阅读 `项目改写完成总结.md`
2. 理解架构设计
3. 修改核心逻辑
4. 添加新功能

---

**📚 希望这份文档能帮助你快速理解项目结构！**

*最后更新：2025-01-02*

