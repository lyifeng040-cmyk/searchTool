# 🚀 极速文件搜索 V2.0 - 快速开始指南

## 📋 目录
1. [环境准备](#环境准备)
2. [编译构建](#编译构建)
3. [运行测试](#运行测试)
4. [使用说明](#使用说明)
5. [常见问题](#常见问题)

## 🔧 环境准备

### 1. 安装Rust
```bash
# Windows (使用rustup)
# 访问 https://rustup.rs/ 下载安装器

# 或使用winget
winget install Rustlang.Rustup

# 验证安装
rustc --version
cargo --version
```

### 2. 安装系统依赖（Windows）
Tauri需要以下依赖：
- **WebView2**：Windows 10/11自带，无需额外安装
- **Visual Studio Build Tools**：
  ```bash
  # 下载并安装 Visual Studio Build Tools
  # 选择"使用C++的桌面开发"工作负载
  ```

### 3. 克隆项目
```bash
cd F:\SearchTool
# 项目已在当前目录
```

## 🏗️ 编译构建

### 开发模式（推荐用于测试）
```bash
# 1. 构建依赖
cargo build

# 2. 运行开发版本
cargo tauri dev

# 特点：
# - 热重载
# - 开发者工具
# - 详细日志
# - 启动较慢
```

### 发布模式（用于分发）
```bash
# 1. 构建发布版本
cargo tauri build

# 2. 输出位置
# - 可执行文件：target/release/filesearch.exe
# - 安装包：target/release/bundle/

# 特点：
# - 完全优化
# - 体积小
# - 性能最佳
# - 无调试信息
```

### 快速构建（仅后端）
```bash
# 只构建Rust后端，不打包
cargo build --release

# 输出：target/release/filesearch.exe
```

## 🧪 运行测试

### 单元测试
```bash
# 运行所有测试
cargo test

# 运行特定模块测试
cargo test database
cargo test scanner
cargo test utils

# 显示详细输出
cargo test -- --nocapture
```

### 集成测试
```bash
# 测试完整搜索流程
cargo test --test integration_tests

# 测试性能
cargo test --release -- --nocapture
```

## 📖 使用说明

### 首次启动

1. **启动应用**
   ```bash
   # 开发模式
   cargo tauri dev
   
   # 或直接运行编译好的exe
   target/release/filesearch.exe
   ```

2. **建立索引**
   - 点击右上角"🔄 立即同步"按钮
   - 等待索引完成（C盘约1-2分钟）
   - 状态栏显示"✅ 索引完成"

3. **开始搜索**
   - 在搜索框输入关键词
   - 点击"🚀 搜索"按钮
   - 查看搜索结果

### 基本操作

#### 搜索模式
```
┌─────────────────────────────────────┐
│ 搜索框                              │
│ ├─ 普通搜索：直接输入关键词         │
│ ├─ 模糊匹配：勾选"模糊"             │
│ ├─ 正则表达式：勾选"正则"           │
│ └─ 实时搜索：勾选"实时"             │
└─────────────────────────────────────┘
```

#### 搜索范围
```
┌─────────────────────────────────────┐
│ 范围选择                            │
│ ├─ 全部驱动器：搜索所有已索引驱动器 │
│ ├─ 单个驱动器：C:、D:等            │
│ └─ 指定目录：点击"📂 选择目录"      │
└─────────────────────────────────────┘
```

#### 结果筛选
```
┌─────────────────────────────────────┐
│ 筛选条件                            │
│ ├─ 格式：按文件扩展名筛选           │
│ ├─ 大小：>1MB, >10MB, >100MB等     │
│ └─ 时间：今天、3天内、7天内等       │
└─────────────────────────────────────┘
```

### 高级功能

#### 1. 批量重命名
```
操作步骤：
1. 选中多个文件（Ctrl+点击）
2. 点击"✏ 批量重命名"按钮
3. 选择重命名模式：
   - 前缀+序号：file001, file002...
   - 文本替换：old -> new
4. 预览效果
5. 执行重命名
```

#### 2. 大文件扫描
```
操作步骤：
1. 点击"📊 大文件"按钮
2. 选择扫描目录
3. 设置最小文件大小（如100MB）
4. 开始扫描
5. 查看结果并排序
```

#### 3. 导出结果
```
操作步骤：
1. 完成搜索
2. 点击"📤 导出"按钮
3. 选择保存位置
4. 生成CSV文件
```

#### 4. 收藏夹
```
操作步骤：
1. 右键点击文件/目录
2. 选择"⭐ 添加收藏"
3. 在收藏下拉框中快速访问
```

### 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Enter` | 开始搜索 |
| `F5` | 刷新搜索 |
| `Esc` | 停止搜索 |
| `Ctrl+A` | 全选 |
| `Ctrl+C` | 复制路径 |
| `Ctrl+Shift+C` | 复制文件 |
| `Delete` | 删除 |
| `Ctrl+E` | 导出结果 |
| `Ctrl+Shift+Space` | 显示迷你窗口 |
| `Ctrl+Shift+Tab` | 显示主窗口 |

## ❓ 常见问题

### Q1: 编译失败 - "链接器错误"
```
问题：找不到MSVC链接器
解决：
1. 安装 Visual Studio Build Tools
2. 选择"使用C++的桌面开发"
3. 重启终端
```

### Q2: 运行失败 - "WebView2未找到"
```
问题：缺少WebView2运行时
解决：
1. Windows 10/11通常自带
2. 手动下载：https://developer.microsoft.com/microsoft-edge/webview2/
```

### Q3: 索引很慢
```
问题：C盘文件太多
解决：
1. 点击"📂 C盘目录"
2. 取消勾选不需要的目录
3. 重建索引
```

### Q4: 搜索结果不准确
```
问题：索引过期
解决：
1. 点击"🔄 立即同步"
2. 等待同步完成
3. 重新搜索
```

### Q5: 内存占用高
```
问题：索引了太多文件
解决：
1. 减少索引范围
2. 清理临时文件
3. 重建索引
```

### Q6: 无法启动 - "端口被占用"
```
问题：开发模式端口冲突
解决：
1. 关闭其他Tauri应用
2. 或使用发布模式运行
```

## 🔍 调试技巧

### 查看日志
```bash
# 设置日志级别
$env:RUST_LOG="debug"
cargo tauri dev

# 或在代码中
env_logger::Builder::from_env(
    env_logger::Env::default().default_filter_or("debug")
).init();
```

### 性能分析
```bash
# 使用cargo-flamegraph
cargo install flamegraph
cargo flamegraph --bin filesearch

# 查看生成的flamegraph.svg
```

### 内存分析
```bash
# 使用valgrind（Linux）
valgrind --tool=massif target/release/filesearch

# 使用Windows Performance Analyzer（Windows）
```

## 📚 进阶配置

### 自定义配置文件
配置文件位置：`~/.filesearch/config.json`

```json
{
  "search_history": [],
  "favorites": [],
  "theme": "light",
  "c_scan_paths": {
    "paths": [
      {
        "path": "C:\\Users\\YourName\\Desktop",
        "enabled": true
      }
    ],
    "initialized": true
  },
  "enable_global_hotkey": true,
  "minimize_to_tray": true,
  "auto_start_watcher": true
}
```

### 数据库优化
```sql
-- 手动优化数据库
sqlite3 ~/.filesearch/index.db

-- 执行优化命令
VACUUM;
ANALYZE;
PRAGMA optimize;
```

### 性能调优
```toml
# Cargo.toml
[profile.release]
opt-level = 3          # 最高优化
lto = true             # 链接时优化
codegen-units = 1      # 单个代码生成单元
strip = true           # 移除符号
panic = "abort"        # 减小二进制大小
```

## 🎯 最佳实践

### 1. 索引策略
- ✅ 首次使用先建立完整索引
- ✅ 定期重建索引（每周一次）
- ✅ 排除不需要的目录
- ❌ 不要索引网络驱动器

### 2. 搜索技巧
- ✅ 使用具体关键词
- ✅ 善用筛选条件
- ✅ 收藏常用目录
- ❌ 避免过于宽泛的搜索

### 3. 性能优化
- ✅ 关闭不需要的功能
- ✅ 定期清理搜索历史
- ✅ 使用分页显示
- ❌ 不要一次加载过多结果

## 🆘 获取帮助

### 文档
- README.md - 项目说明
- 项目改写完成总结.md - 技术细节
- 本文档 - 快速开始

### 社区
- GitHub Issues - 问题反馈
- Discussions - 讨论交流

### 联系方式
- Email: your@email.com
- GitHub: @yourusername

---

**🎉 祝你使用愉快！**

**⚡ 极速搜索，触手可及！**

